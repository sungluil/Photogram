language: java
jdk:
  - openjdk8

branches:
  only:
    - master

#travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.mvnw'

script: "./mvnw package"

#CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - josungluil@naver.com
